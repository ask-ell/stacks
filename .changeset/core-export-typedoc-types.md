---
'@ask-ell/core': patch
---

Exported `CommonMethodParams`, `ISuccessResult`, `IFailResult`, and `FoldParams` so they resolve correctly in generated documentation instead of appearing as unlinked types.
