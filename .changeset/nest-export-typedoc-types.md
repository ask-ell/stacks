---
'@ask-ell/nest': patch
---

Exported `NestSentryClientFactory`, `NestSentryConfigurationProps`, and `ServiceWithSubscriptionsParams` so they resolve correctly in generated documentation instead of appearing as unlinked types.
