---
'@ask-ell/react': patch
---

Exported `SelectEvent`, `FormDataFieldUpdater`, `SubmitTrigger`, `ReactSentryClientFactory`, and `ReactSentryConfigurationProps` so they resolve correctly in generated documentation instead of appearing as unlinked types.
