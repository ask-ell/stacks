---
'@ask-ell/github': patch
---

Exported `./types` and the repository DTO interfaces from the package entry point so they resolve correctly in generated documentation instead of appearing as unlinked types.
