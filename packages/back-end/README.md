# back-end

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build back-end` to build the library.

## Running unit tests

Run `nx test back-end` to execute the unit tests via [Jest](https://jestjs.io).
