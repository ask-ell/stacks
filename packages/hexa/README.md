# hexa

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build hexa` to build the library.

## Running unit tests

Run `nx test hexa` to execute the unit tests via [Jest](https://jestjs.io).
